node_modules/
dist/
*.zip
.build/
.js13k-check/
.gen-img/
media/.js13k-images/
*.log
.DS_Store

# Outside the submission: design studies, the comparison variant, tools whose
# job is done, and the full-resolution originals of the artwork. Kept locally,
# not in the public repo.
labs/
src/index-nofx.html
tools/harmony.js
tools/skill.js
tools/music.js
media/*-source.png

# The machine's global gitignore excludes package-lock.json. Here it has to be
# versioned: the js13k rules ask for a public repo the game rebuilds from, and
# the lockfile is what pins the terser and roadroller versions.
!package-lock.json

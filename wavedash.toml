# Wavedash challenge configuration (js13kGames 2026).
# The game bundles no SDK: the platform injects the `Wavedash` global before the
# game code runs, and src/index.html only calls it behind a guard.
game_id = "j97b5vbzt29sdkpz7w5g4c6cnx8e7khh"

# The Wavedash target is the unminified page: no size limit here, so no
# roadroller, so no decode delay at load time.
upload_dir = "dist/wavedash"
entrypoint = "index.html"
